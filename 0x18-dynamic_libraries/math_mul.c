#include "main.h"

/**
 * mul - takes in two intergers and multiplies them
 *
 * @a: fist integer
 * @b: second integer
 *
 * Return: the result of the multiplication
 */

int mul(int a, int b)
{
	return (a*b);
}
